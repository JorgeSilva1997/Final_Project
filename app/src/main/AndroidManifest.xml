<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/apm_logo"
        android:label="CRA - APM"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".List_User"
            android:label="@string/title_activity_list__user"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".LOGIN.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LOGIN.Login_Admin" />
        <activity android:name=".Regist" />
        <activity android:name=".MAIN.Main" />
        <activity android:name=".MAIN.Main_admin" />
        <activity android:name=".Perfil_User" />
        <activity android:name=".PAVILHAO.Pavilhao" />
        <activity android:name=".PROVA.Prova" />
        <activity android:name=".ESCALOES.Escalao" />
    </application>

</manifest>